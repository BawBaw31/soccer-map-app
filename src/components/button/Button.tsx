import React from 'react'
import * as Styled from './Button.styles'

export const Button: React.FunctionComponent = () => {
    return <Styled.ButtonContainer>Add your stadium</Styled.ButtonContainer>
}
